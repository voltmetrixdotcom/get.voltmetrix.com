# download voltmetrix-cli from github and install it to /usr/local/bin

# download voltmetrix-cli

wget https://get.voltmetrix.com/voltmetrix -O /usr/local/bin/voltmetrix

# make it executable

chmod +x /usr/local/bin/voltmetrix

# done

echo "Voltmetrix CLI installed to /usr/local/bin > refer to https://voltmetrix.com/docs to learn how to use it."
